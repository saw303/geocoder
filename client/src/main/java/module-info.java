module silviowangler.address.client {

    requires jdk.incubator.httpclient;
    requires gson;

    requires silviowangler.address.client.api;

    requires java.sql;

    requires google.maps.services;
}